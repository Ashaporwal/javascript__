//WAP to calculate simple intrest where principal amount rate of intrest and time duration is taken from user input 
let p = 1, r = 1, t = 1;
let si = (p * t * r) / 100;
console.log("Simple Interest = " + si);